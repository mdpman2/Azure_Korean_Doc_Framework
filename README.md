# Azure Korean Document Understanding & Retrieval Framework

이 프로젝트는 Azure AI Services (Document Intelligence, OpenAI)를 활용하여 한국어 문서를 정밀하게 분석하고, RAG (Retrieval-Augmented Generation) 시스템을 위한 최적의 청킹(Chunking) 데이터를 생성 및 관리하는 프레임워크입니다.

## 🌟 핵심 기능: Context-Rich Rolling Window

단순한 텍스트 분할이 아닌, 문서의 **구조(Hierarchy)**와 **문맥(Context)**을 보존하는 **"Context-Rich Rolling Window"** 전략을 사용합니다.

### 📄 문서 파싱 (Parsing)

- **구조적 하이브리드 파싱**: Azure Document Intelligence Layout 모델(`prebuilt-layout`)을 사용하여 헤더, 본문, 표, 이미지를 구분합니다.
- **계층적 문맥 주입**: 모든 청크에 Breadcrumb(상위 목차 경로)를 자동으로 주입하여 검색 품질을 향상시킵니다.
- **표 독립화 (Table Isolation)**: 표 데이터를 별도의 청크로 격리하여 Markdown 형식으로 구조를 유지합니다.
- **한국어 특화 청킹**: `kss`를 활용한 한국어 문장 분리 및 의미 단위 분할을 지원합니다.
- **Visual RAG**: 문서 내 이미지를 GPT-4.1 Vision으로 분석하여 텍스트로 통합합니다.

### 🖼️ 향상된 이미지 분석 (v2.0)

- **이미지 유형별 분석 가이드**: UI 스크린샷, 표 이미지, 다이어그램/순서도, 차트/그래프 등 유형에 따른 맞춤 분석
- **문맥 기반 이미지 설명**: 이미지 주변 텍스트를 분석하여 문서 맥락에 맞는 설명 생성
- **한국어 최적화**: 모든 이미지 설명을 자연스러운 한국어로 생성

### 📊 향상된 표 처리 (v2.0)

- **표 전용 청크 분리**: 표 데이터를 독립적인 청크로 분리하여 검색 정확도 향상
- **Markdown 구조 보존**: 표의 행/열 구조를 Markdown 테이블로 정확하게 변환
- **메타데이터 태깅**: `is_table_data`, `contains_table` 플래그로 표 콘텐츠 식별

### 🔢 번호 목록 처리 (v2.0)

- **정책 문서 최적화**: "06 제목" → "### 06. 제목" 형식으로 자동 변환
- **계층 구조 인식**: 번호 기반 목차 및 섹션 구조 자동 파악
- **메타데이터 태깅**: `contains_numbered_section` 플래그로 번호 섹션 식별

## ⚡ 주요 특징

- **고성능 병렬 처리**: 다중 코어를 활용하여 대량의 문서를 빠르게 인덱싱합니다. (`--workers` 옵션)
- **변경 감지 인덱싱**: 파일의 해시(Hash)를 비교하여 변경된 파일만 선택적으로 처리합니다.
- **멀티 모델 평가**: 동일한 질문에 대해 다양한 모델(GPT-4.1, o1 등)의 답변을 비교 테스트할 수 있습니다.
- **유형별 청크 통계**: 처리 완료 시 텍스트/표/이미지 청크 수를 분류하여 표시합니다.

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                        doc_chunk_main.py                        │
│                     (CLI 통합 실행 스크립트)                      │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Document Processing                         │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────┐  │
│  │   parser.py     │───▶│   chunker.py    │───▶│ vector_store│  │
│  │ (Azure DI +     │    │ (Context-Rich   │    │    .py      │  │
│  │  GPT-4.1 Vision)│    │  Rolling Window)│    │(Azure Search)│  │
│  └─────────────────┘    └─────────────────┘    └─────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Q&A / RAG                               │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────────┐    ┌─────────────────┐                     │
│  │    agent.py     │◀───│  Azure AI Search │                    │
│  │  (RAG 로직)      │    │  (Vector Search) │                    │
│  └─────────────────┘    └─────────────────┘                     │
└─────────────────────────────────────────────────────────────────┘
```

## 🚀 시작하기

### 설치

```bash
pip install openai azure-ai-documentintelligence azure-search-documents \
    pymupdf pillow python-dotenv tiktoken kss
```

### 환경 변수 설정 (.env)

```env
# Azure OpenAI
AZURE_OPENAI_API_KEY=your_openai_api_key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Azure Document Intelligence
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
AZURE_DOCUMENT_INTELLIGENCE_KEY=your_doc_intel_key

# Azure AI Search
AZURE_SEARCH_SERVICE_ENDPOINT=https://your-search.search.windows.net
AZURE_SEARCH_INDEX_NAME=your-index-name
AZURE_SEARCH_API_KEY=your_search_api_key
```

## 🛠 사용 방법

### 1. 문서 인덱싱 (Ingestion)

```bash
# 디렉토리 내 모든 PDF 처리 (기본 3개 병렬 작업)
python doc_chunk_main.py --path "RAG_TEST_DATA"

# 특정 파일만 처리 및 병렬 작업 수 지정
python doc_chunk_main.py --path "data/sample.pdf" --workers 5

# Q&A 없이 인덱싱만 수행
python doc_chunk_main.py --path "data/sample.pdf" --skip-qa
```

### 2. Q&A 테스트 및 모델 비교

```bash
# 인덱싱 후 바로 테스트 (기본 질문 사용)
python doc_chunk_main.py --path "RAG_TEST_DATA"

# 인덱싱 없이 특정 질문으로 테스트
python doc_chunk_main.py --skip-ingest --question "올해의 경제 전망은?"

# 특정 모델로 테스트
python doc_chunk_main.py --skip-ingest --question "늘봄학교란?" --model "gpt-4.1"
```

### 3. 출력 예시

```
✅ 처리 완료
   └── 총 세그먼트: 66개
   └── 생성된 청크: 30개
       ├── 텍스트: 23개
       ├── 표: 5개
       └── 이미지: 2개
```

## 📂 프로젝트 구조

```
azure_korean_doc_framework1/
├── azure_korean_doc_framework/
│   ├── parsing/
│   │   ├── parser.py       # Azure DI + GPT-4.1 Vision 하이브리드 파싱
│   │   └── chunker.py      # Context-Rich Rolling Window 청킹
│   ├── core/
│   │   ├── vector_store.py # Azure AI Search 인덱싱/검색
│   │   └── agent.py        # RAG 질의응답 로직
│   ├── utils/
│   │   ├── logger.py       # 청크 로그 저장
│   │   └── azure_clients.py # Azure 클라이언트 초기화
│   └── config.py           # 환경 변수 검증 및 모델 구성
├── doc_chunk_main.py       # 통합 실행 스크립트 (CLI)
├── doc_chunk_main_only_q.py # Q&A 전용 스크립트
├── verify_index.py         # 인덱스 검증 스크립트
├── output/                 # 청크 로그 출력 폴더
├── .env                    # 환경 변수 설정
└── README.md
```

## 🔧 핵심 모듈 설명

### parser.py (문서 분석)

| 함수 | 설명 |
|------|------|
| `parse_pdf()` | PDF를 Azure DI로 분석하여 구조화된 세그먼트 추출 |
| `_describe_image()` | GPT-4.1 Vision으로 이미지 분석 (유형별 가이드 적용) |
| `_extract_context_around_offset()` | 이미지 주변 텍스트 추출로 문맥 파악 |
| `_enhance_numbered_content()` | 번호 목록 형식 정규화 ("06 제목" → "### 06. 제목") |

### chunker.py (청킹)

| 함수 | 설명 |
|------|------|
| `create_chunks()` | 세그먼트를 Context-Rich 청크로 변환 |
| `_extract_special_chunks()` | 표/이미지 전용 청크 분리 |
| `_rolling_window_chunk()` | 문맥 보존 롤링 윈도우 청킹 |
| `_split_korean_sentences()` | kss 기반 한국어 문장 분리 |

### vector_store.py (벡터 저장소)

| 함수 | 설명 |
|------|------|
| `upload_documents()` | 청크를 Azure AI Search에 업로드 |
| `hybrid_search()` | 키워드 + 벡터 하이브리드 검색 |
| `ensure_index_exists()` | 인덱스 자동 생성/검증 |

## 📈 청크 메타데이터

각 청크에는 다음 메타데이터가 포함됩니다:

| 필드 | 설명 |
|------|------|
| `chunk_type` | 청크 유형 (text, table, image) |
| `breadcrumb` | 상위 섹션 경로 (예: "1장 > 1.1절") |
| `source_file` | 원본 파일명 |
| `page_number` | 페이지 번호 |
| `is_table_data` | 표 데이터 여부 |
| `is_image_data` | 이미지 설명 여부 |
| `contains_numbered_section` | 번호 섹션 포함 여부 |

## 🧪 테스트 결과 예시

**질문**: "늘봄학교란 무엇이고 언제부터 전국 도입되나요?"

**답변** (문서 기반):
> 늘봄학교는 초등학교에서 정규수업 외에 다양한 교육 및 돌봄 프로그램을 제공하는 종합 돌봄 서비스입니다.
> - **2024년 1학기**: 2,000개교에서 시범 운영
> - **2024년 2학기**: 전국 모든 초등학교로 확대
> - **2025년**: 전 학년 확대 및 내실화
>
> 📚 출처: [2024년 교육부 업무계획.pdf]

## 📋 요구사항

- Python 3.9+
- Azure 구독 및 다음 서비스:
  - Azure OpenAI (GPT-4.1, text-embedding-3-small)
  - Azure Document Intelligence
  - Azure AI Search

## 📜 라이선스

MIT License

## 🔗 관련 프로젝트

- [Korean_Doc_Chunking_Azure](../Korean_Doc_Chunking_Azure) - 이전 버전
- [azure_search_foundry_agent.py](../azure_search_foundry_agent.py) - Foundry Agent 통합 버전
