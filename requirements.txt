# azure_korean_doc_framework — 의존성 패키지
# 설치(권장, uv): uv pip install -r requirements.txt
#   또는 프로젝트 동기화: uv sync   (pyproject.toml 기반)
# 참고: 의존성의 단일 출처는 pyproject.toml이며, 이 파일은 호환용입니다.

# ===== Azure SDK =====
openai>=1.109.0
azure-ai-documentintelligence>=1.0.0
azure-search-documents>=11.6.0

# ===== 문서 처리 =====
pymupdf>=1.26.0
pillow>=12.0.0
tiktoken>=0.12.0

# ===== 한국어 NLP =====
kss>=6.0.0

# ===== Graph RAG (v4.0) =====
networkx>=3.4

# ===== [v6.0] HTTP 클라이언트 (Agentic Retrieval, Content Understanding) =====
requests>=2.32.0

# ===== [v6.0] Neo4j Graph DB (선택 설치: GRAPH_STORAGE_BACKEND=neo4j 사용 시) =====
# uv pip install neo4j     (또는: uv sync --extra neo4j)
# neo4j>=5.20.0

# ===== Reranker (v5.1 — LightRAG 참조) =====
# Cross-Encoder 로컬 reranker (선택 설치: RERANKER_BACKEND=cross_encoder 사용 시)
# uv pip install sentence-transformers     (또는: uv sync --extra reranker)
# Jina Reranker는 API 기반이므로 추가 패키지 불필요

# ===== 유틸리티 =====
python-dotenv>=1.0.0
